# 标签栏自动化验收

验收日期：2026-09-08。用户已手动编译并确认 iOS 26、27 正常，本轮对相同生产代码补做自动操作、录屏检查和代码审查。

## 结论

**尚未完全通过，未提交或推送到任何仓库。** 稳定状态正常，短暂过渡仍可观察到以下现象：

- iOS 26.5：软件源刷新时进入搜索，地球已向左移动，红圈在旧的完整标签位置停留几帧，再回到地球右上方。没有持续裁切或长时间图标缺失。
- iOS 27.0：同一路径中，自有黑色地球与系统灰色地球在相邻少数采样帧存在位置差，呈短暂重影；随后正常。不是持续出现两个图标。

上述结论来自本机模拟器录屏，不否定用户真机操作时未察觉异常的结果。本轮没有据此继续修改生产代码。

## 环境与执行方式

- 两套系统均使用标准 iPhone 17 Pro 模拟器；应用为当前工作区构建的 `Sileo Demo` Debug。
- XcodeBuildMCP 自动执行标签点击，以新的页面快照确认实际选择结果，并使用模拟器录屏检查过渡像素。
- Device Hub 保持运行。Mac 锁屏时其窗口无法接管；自定义 SE2 设备的触摸无响应，这些操作不计入验收。换用标准 iPhone 17 Pro 后两套系统均有有效的页面切换。
- 为延长刷新，模拟器使用用户提供列表中的 82 个普通软件源；特殊发行格式的两个源未导入。未修改真实设备上的软件源。
- 刷新通过临时诊断 dylib 调用页面现有 `refreshSources:` 方法启动；正式应用没有加入该 dylib。切换录屏期间没有开启逐帧图层采样。自动操作未可靠触发下拉刷新，不将其计为下拉手势验证。
- 当前源列表有失效源，刷新结束会出现错误结果页。该页已关闭；被弹窗遮挡的点击不计入通过。

## 已完成的检查

| 项目 | 结果 |
| --- | --- |
| 当前工作区模拟器构建 | 通过；7.1 秒，只有现有脚本缺少输出声明的警告 |
| 软件源刷新 → 搜索 → 软件源 | 两套系统均实际完成；过渡像素问题见结论 |
| 软件源刷新期间，软件包 → 搜索 → 软件包 | 两套系统均实际完成；稳定后标签与角标正常 |
| 新闻 → 搜索 → 新闻 | 两套系统均实际完成 |
| 精选 → 搜索 → 精选 | 两套系统均实际完成 |
| 刷新结束后的结果页关闭与角标消失 | 两套系统均观察到 |
| 代码审查、剩余旧标签访问器检查、git diff --check | 通过，未发现新的业务访问遗漏或格式错误 |
| iPad、所有 iOS 26 小版本、深浅色和队列完整回归 | 本轮未覆盖 |

本轮复用了用户已经生成的 deb / IPA，没有重复打包。新的自动化验收以模拟器构建为准，不用“编译成功”替代动画验收。

## 可复核证据

原始文件保存在 Git 忽略的 `build/tab-acceptance-20260908/`：

- `ios26-source-refresh.mp4`：刷新及两类往返操作。软件源进入搜索约在 3.2 秒附近发生。
- `ios26-refresh-transition.mp4`：上述过渡的底部局部片段，便于直接复看。
- `26source-enter-detail.png`：从 2.65 秒起以 30 fps 取样，按从左到右、从上到下排列；第 4 行可看到红圈与移动地球分离，之后恢复。
- `ios27-source-refresh.mp4`：iOS 27 同组操作，约 4.6 秒附近进入搜索。
- `27source-enter-detail.png`：从 4.2 秒起以 30 fps 取样；第 3 行末尾可见短暂双影。
- `ios27.mp4`、`ios26-other-tabs.mp4`：未刷新时的其它标签往返。
- `source-hashes.json`：验收时 17 个已修改跟踪文件的 SHA-256。
- `pre-sync.patch`：验收前完整未提交改动备份。

帧图使用 30 fps 取样；不能把这些帧数当成真机原始显示帧数，也不能把多次切换合并成一次长时间异常。

## 同步状态

已获取内网及 GitHub 的 `main`，两者与本地 HEAD 同为 `a243189c14fc083fda735d7e8fd81c2417ad2992`。尚未创建提交，也未推送。

拟同步的功能代码包括标签栏修复、新标签 API 访问适配、Demo IPA 构建入口和相关说明。本机签名团队、断点、仅排序的 plist / 工程变化保留本地。诊断原始文件和安装包不进入代码提交。

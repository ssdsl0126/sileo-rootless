# 标签栏运行时诊断

## 基线与范围

代码已在重启前恢复到用户认可的稳定基线；后续局部修复仅调整 `TabBar.swift` 中前景装饰的位置和交接。`TabBarController.swift` 仍与检查点中的稳定基线一致。下拉刷新逻辑未因工具手势失败而修改。

运行时诊断使用 Xcode beta 的 Device Hub 环境、模拟器专用控制接口、屏幕录制及临时注入采样器。采样器未加入工程或正式安装包。

## 已确认的机制

1. UIKit 在标签栏重挂载期间可能返回已脱离窗口的 presentation layer。跨不连通图层树换算会丢失标签栏偏移：原本屏幕底部的 `(39, 602)` 曾被换算成 `(39, 18)`。当前代码只接受与窗口相连的 presentation layer。
2. 完整菜单与紧凑搜索之间，原始 UIImageView 的位置可能已经到达终点；真正移动的是窗口其它分支里的 morph 副本。不能仅靠原图像的 model 坐标或 alpha 推断屏幕像素。
3. iOS 26.5 的移动图标位于 `_UIMagicMorphView`；iOS 27 的外层是 `UIKit.MagicMorphView`，内部图标下沉到 `AnimationKit.MagicMorphLayer`。当前代码统一读取这些实际动画层的位置。
4. iOS 26 太早在最终左侧位置补绘会造成重影；iOS 27 的原图标又会在交接时淡出，因此两种实际渲染路径的补绘时机不同。当前代码按检测到的图层类型处理，不增加新的布局控制器。

既有证据位于 `build/tab-diagnosis/checkpoint-20260907/`，包括稳定基线、已撤回版本、录屏和 JSON。关键采样包括 `refresh26-c.json`（脱离窗口的显示层）、`magic26.json`（真实移动图标）和 `layers27.json`（AnimationKit 图标层）。

## 2026-09-08 验收发现的剩余问题

详见 `TABBAR-VALIDATION.md`。当前代码在两套系统稳定状态正常，但新录屏仍显示：iOS 26 刷新红圈在收缩开始时短暂落后于地球；iOS 27 原图与补绘图在过渡开始时有少数帧错位重影。因此不能宣布所有过渡问题均已解决。

下一步定位应针对这些具体帧：

- 核对紧凑容器是按 model 尺寸还是正在变化的 presentation 尺寸识别，是否导致早期仍选择完整标签的坐标。
- 核对 morph 匹配使用的终点矩形与原图当前显示矩形是否属于同一语义，避免拿移动中的矩形去匹配终点。
- 对照系统合成提交与 CADisplayLink 采样时序，判断 iOS 27 的双影是两份图像绘制时机还是位置读取造成。

这些是后续调查方向，尚未用本轮同步采样确认，不能直接当成已证实的新根因或据此继续改代码。

## 其它诊断记录

早期大量软件源刷新时曾遇到后台 `EvanderDownloader.container` / `progressCallback` 路径的内存访问崩溃；栈位于下载队列，未归因于标签栏，也未在本轮修改该后台路径。本轮记录的有效往返流程完成，不以此声称整个下载系统已通过稳定性测试。
